要求に従い、修正後のmain.pyファイルをかけ。

要求：
    どの段階の処理をしたいか選択できるようにせよ。
    処理１：
        シミュレーションを行い、出力の時系列データを.csvファイルに保存せよ。
        保存した.csvファイルを.wavファイルに変換せよ。
    処理2：
        .csvファイルを選択し、読み込め。
        読み込んだ.csvファイルを.wavファイルに変換し、保存せよ。
    処理3：
        オーディオファイルを選択し、読み込め。
        読み込んだオーディオデータを.csvファイルに変換し、保存せよ。
    処理4：
        .csvファイルを二つ選択し、読み込め。
        二つの.csvの振る舞いをcompare_signals関数で比較せよ。
    
    不要なヘッダーは、削除せよ。
    可読性の良いコードにせよ。
    進捗がわかるようにprintで英語で示せ。
    変数名をわかりやすくせよ。
    コメントは英語にせよ。

main.py：
